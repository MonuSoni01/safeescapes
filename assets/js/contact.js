import{saveForm}from'./forms.js';const f=document.querySelector('[data-firestore-form]');f?.addEventListener('submit',async e=>{e.preventDefault();const btn=f.querySelector('button[type=submit]');btn.disabled=true;btn.textContent='Submitting…';try{const data=Object.fromEntries(new FormData(f));await saveForm(f.dataset.firestoreForm,data);f.reset();showToast('Thank you. Your details have been received.')}catch(err){console.error(err);showToast('Could not submit. Please try again.')}finally{btn.disabled=false;btn.textContent='Submit →'}});
